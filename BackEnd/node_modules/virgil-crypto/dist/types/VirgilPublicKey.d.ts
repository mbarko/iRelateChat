/// <reference types="@virgilsecurity/core-foundation" />
import { IPublicKey } from './types';
export declare class VirgilPublicKey implements IPublicKey {
    readonly identifier: Uint8Array;
    readonly lowLevelPublicKey: FoundationModules.PublicKey;
    private _isDisposed;
    get isDisposed(): boolean;
    get key(): Uint8Array;
    constructor(identifier: Uint8Array, lowLevelPublicKey: FoundationModules.PublicKey);
    dispose(): void;
}
